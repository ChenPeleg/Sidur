# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Production on merge
on:
  push:
    branches: [ "production" ]

jobs:
  deploy_with_trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: production
      - name: list all filesS
        run: ls -al
      - name: Trigger the repository dispatch event
        run: |
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "authorization: Bearer ${{ secrets.DEPLOY_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/ChenPeleg/app-links-portal/actions/workflows/deploy-trigger.yml/dispatches \
          -d '{"ref":"main","inputs":{"name":"my name","repo": "${{ github.repository }}","branch":"production","command" : "build:site", "dist" :"dist1" "custom":"custom data"}}'
 

